import { useTranslation } from '../../hooks/useTranslation';
import { useForm } from '../../hooks/useForm';
import React from 'react';

export const MobXForm = () => {
    return (
    <form>
      {/* form fields go here */}
    </form>
  );
};